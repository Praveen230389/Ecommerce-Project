apiVersion: v1
kind: Service

metadata:
  name: analytics-service
  namespaces: dev

spec:
  selector:
    app: analytics-service

  ports:
  - protocol: TCP
    port: 3015
    targetPort: 3015

  type: ClusterIP
