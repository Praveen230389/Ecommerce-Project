apiVersion: v1
kind: Service

metadata:
  name: user-service
  namespaces: dev

spec:
  selector:
    app: user-service

  ports:
  - protocol: TCP
    port: 3013
    targetPort: 3013

  type: ClusterIP
