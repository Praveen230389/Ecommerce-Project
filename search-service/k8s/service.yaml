apiVersion: v1
kind: Service

metadata:
  name: search-service
  namespaces: dev

spec:
  selector:
    app: search-service

  ports:
  - protocol: TCP
    port: 3011
    targetPort: 3011

  type: ClusterIP
